<h1>Item: <%= @item.title %> </h1>

<h2>Description: <%= @item.description %></h2> 
<h2>Completed: <%= @item.completed %></h2>

<center>
	<%= link_to "Edit item", edit_item_url(@item.id), class: "btn" %>
	<%= link_to "Delete item", item_url(@item), method: :delete, class: "btn" %>
</center>